{
  "name": "dlts-readium-js-viewer",
  "version": "0.1.0",
  "description": "DLTS ReadiumJS viewer",
  "dependencies": {
    "js-beautify": "^1.6.4",
    "mocha": "^3.0.2",
    "webdriverio": "^4.2.16"
  },
  "scripts": {
    "clean": "rm -fr readium-js-viewer/ tmp/*",
    "dist": "npm run clean && scripts/dist.sh",
    "dist:verify": "scripts/dist-verify.sh",
    "test": "npm run test:dist && npm run test:mocha",
    "test:dist": "npm run dist && npm run dist:verify",
    "test:mocha": "node node_modules/mocha/bin/mocha tests/"
  }
}
